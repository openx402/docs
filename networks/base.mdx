---
title: "Base Network"
description: "OpenX402 facilitator guide for Base (Ethereum L2)"
---

## Overview

Base is OpenX402's **primary facilitator network**. As Coinbase's Ethereum Layer 2, it offers the perfect balance of security, cost, and performance for micropayments.

## Facilitator URL

```
https://openx402.ai/base
```

## Network Details

| Property | Value |
|----------|-------|
| **Chain ID** | 8453 |
| **RPC URL** | https://mainnet.base.org |
| **Native Token** | ETH |
| **Payment Token** | USDC |
| **Block Time** | ~2 seconds |
| **Transaction Cost** | ~$0.01 |
| **Status** | ✅ Production |

## Why Base?

<CardGroup cols={2}>
  <Card title="Ethereum Security" icon="shield">
    Inherits security from Ethereum mainnet
  </Card>
  <Card title="Low Costs" icon="dollar-sign">
    Significantly cheaper than L1 Ethereum
  </Card>
  <Card title="Fast Settlement" icon="bolt">
    2-second block times for quick confirmations
  </Card>
  <Card title="Large Ecosystem" icon="users">
    Access to Coinbase's user base and liquidity
  </Card>
</CardGroup>

## Quick Start

### 1. Install Dependencies

<CodeGroup>
```bash npm
npm install ethers @openzeppelin/contracts
```

```bash yarn
yarn add ethers @openzeppelin/contracts
```
</CodeGroup>

### 2. Configure Network

```javascript
const baseConfig = {
  chainId: 8453,
  name: 'Base',
  rpcUrl: 'https://mainnet.base.org',
  facilitatorUrl: 'https://openx402.ai/base',
  usdcAddress: '0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913'
};
```

### 3. Create Payment

```javascript
import { ethers } from 'ethers';

async function createBasePayment(amount, recipient) {
  const provider = new ethers.JsonRpcProvider(baseConfig.rpcUrl);
  const signer = new ethers.Wallet(privateKey, provider);

  // USDC contract on Base
  const usdc = new ethers.Contract(
    baseConfig.usdcAddress,
    ['function transfer(address to, uint256 amount)'],
    signer
  );

  // Send payment
  const tx = await usdc.transfer(
    recipient,
    ethers.parseUnits(amount.toString(), 6) // USDC has 6 decimals
  );

  await tx.wait();
  return tx.hash;
}
```

### 4. Verify with Facilitator

```javascript
async function verifyBasePayment(txHash) {
  const response = await fetch('https://openx402.ai/base/verify', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      txHash: txHash,
      network: 'base'
    })
  });

  return await response.json();
}
```

## Starter Repository

Get started quickly with our Base starter template:

```bash
git clone https://github.com/openx402/base-starter
cd base-starter
npm install
npm run dev
```

<Card title="Base Starter Repo" icon="github" href="https://github.com/openx402/base-starter">
  Complete example application with Base integration
</Card>

## Server-Side Integration

### Express.js Example

```javascript
const express = require('express');
const app = express();

app.post('/api/premium', async (req, res) => {
  const { txHash } = req.body;

  // Verify payment through Base facilitator
  const verification = await fetch('https://openx402.ai/base/verify', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      txHash,
      network: 'base',
      expectedAmount: '1.00' // 1 USDC
    })
  }).then(r => r.json());

  if (!verification.valid) {
    return res.status(402).json({ error: 'Payment required' });
  }

  // Payment verified - deliver service
  res.json({
    data: 'Your premium content',
    transactionId: verification.transactionId
  });
});

app.listen(3000);
```

## Testing

Use Base Sepolia testnet for development:

```javascript
const baseSepoliaConfig = {
  chainId: 84532,
  name: 'Base Sepolia',
  rpcUrl: 'https://sepolia.base.org',
  facilitatorUrl: 'https://openx402.ai/base/test',
  usdcAddress: '0x036CbD53842c5426634e7929541eC2318f3dCF7e'
};
```

Get testnet ETH from [Base Sepolia Faucet](https://portal.cdp.coinbase.com/products/faucet).

## Endpoints

| Endpoint | Purpose | Cost |
|----------|---------|------|
| `/open` | Production payments | $1 USDC |
| `/test` | Development testing | $0.0001 USDC |
| `/verify` | Payment verification | Free |
| `/settle` | Settlement finalization | Free |
| `/supported` | Network info | Free |

## Best Practices

<AccordionGroup>
  <Accordion title="Gas Optimization">
    - Use batch transactions when possible
    - Set appropriate gas limits (21000 for transfers)
    - Monitor gas prices and adjust timing
  </Accordion>

  <Accordion title="Error Handling">
    - Implement retry logic for network issues
    - Handle insufficient balance gracefully
    - Log all transaction attempts
  </Accordion>

  <Accordion title="Security">
    - Never expose private keys client-side
    - Validate all payment amounts server-side
    - Implement rate limiting on endpoints
  </Accordion>
</AccordionGroup>

## Resources

<CardGroup cols={2}>
  <Card title="Base Documentation" icon="book" href="https://docs.base.org">
    Official Base network documentation
  </Card>
  <Card title="Base Block Explorer" icon="magnifying-glass" href="https://basescan.org">
    View transactions and contracts
  </Card>
  <Card title="Base Bridges" icon="bridge" href="https://docs.base.org/docs/tools/bridges">
    Third-party bridges to Base (Superbridge, Relay)
  </Card>
  <Card title="X402 Scan" icon="chart-line" href="https://www.x402scan.com/facilitator/openx402">
    Monitor OpenX402 transactions
  </Card>
</CardGroup>

## Support

- **Network Status**: [Base Status](https://status.base.org)
- **OpenX402 Support**: [X @openx402](https://x.com/openx402)
- **Testnet Faucet**: [Coinbase Developer Platform](https://portal.cdp.coinbase.com/products/faucet)
